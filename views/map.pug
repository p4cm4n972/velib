include head
include footer

h1 Map des vélibs de Paris
div(id="maps-canvas" style="border:solid red 2px; width:700px; height:350px; margin:auto")
script(src='https://maps.googleapis.com/maps/api/js?key=AIzaSyBA3aPQ0M3lmwom2xMr44K8AnPOug84ENI')
script.
  google.maps.event.addDomListener(window, 'load' , initialize);
  function initialize() {
  var gps = navigator.geolocation.getCurrentPosition(
  function(position) {
  var mapOptions = {
  center: { lat: position.coords.latitude, lng: position.coords.longitude},
  zoom: 16
  };
  var map = new google.maps.Map(document.getElementById('maps-canvas'),mapOptions);
  };
  );
  setMarkers(map);
  }
  var velib = !{velibStrArray};
  function setMarkers(map) {
  for(var i = 0; i <= velib.length; i++) {
  var marker = new google.maps.marker({
  position: {lat: velib[i].latitude,lng: velib[i].longitude},
 map:map
 })
 }
 }
